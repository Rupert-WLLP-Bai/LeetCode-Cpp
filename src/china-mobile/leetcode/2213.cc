#include <vector>
#include <string>

using namespace std;

// 第 i 个查询会将 s 中位于下标 queryIndices[i] 的字符更新为 queryCharacters[i]
// 返回一个长度为 k 的数组 lengths,
// 其中lengths[i]是在执行第i个查询之后s中仅由单个字符重复组成的最长子字符串的长度

class Solution {
  public:
    vector<int> longestRepeating(string s, string queryCharacters,
                                 vector<int> &queryIndices) {
        // 模拟
        vector<int> res;
        string str;
    }

    int repeatLength(const string &s) {
        // s[0]
        // find last of
    }

    void update(string &str, const string &queryCharacters,
                const vector<int> &queryIndices) {
    }
};

// 2026.08.13